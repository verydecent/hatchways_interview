import React from "react";

const SearchBar = ({}) => {
  return (
    <div className="searchbar">
      <input placeholder="Search by name" className="searchbar-input" />
    </div>
  );
};

export default SearchBar;
